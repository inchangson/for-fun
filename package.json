{
  "name": "tokyo-travel-journal", "version": "1.0.0", "private": true, "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1", "build": "node scripts/validate.mjs && vite build && node scripts/audit.mjs",
    "preview": "vite preview --host 127.0.0.1", "encrypt": "node scripts/encrypt.mjs",
    "test": "node --test tests/*.test.mjs", "test:e2e": "playwright test"
  },
  "dependencies": {"leaflet": "^1.9.4"},
  "devDependencies": {"vite": "^7.3.0", "@playwright/test": "^1.58.0"}
}
